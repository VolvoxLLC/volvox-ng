export interface II18n {
    version: string;
    volvox: {
        components: {
            confirmDialog: {
                title: string;
                message: string;
            }
        }
        commons: {
            buttons: {
                save: string;
                deny: string;
                dismiss: string;
                confirm: string;
                cancel: string;
            }
        };
    };
}
