export interface II18n {
    version: string;
    volvox: {
        components: {
            confirmDialog: {
                title: string;
                message: string;
            }
        }
        commons: {
            buttons: {
                save: string;
                deny: string;
                confirm: string;
                cancel: string;
            }
        };
    };
}
